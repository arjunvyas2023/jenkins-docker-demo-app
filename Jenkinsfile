/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
  
    stages {
        stage('main') {
            steps {
                echo 'main'
            }
        }
         stage('dev') {
            steps {
                echo 'dev'
            }
        }
         stage('feature-one') {
            steps {
                echo 'feature-one'
            }
        }
    }
}
